# [PT] Manacá — Dependências extras da imagem GPU de PRÉ-TREINAMENTO (Fase 2b)
# [EN] Manacá — Extra dependencies of the PRE-TRAINING GPU image (Phase 2b)
# ------------------------------------------------------------------------
# [PT] A base (docker/Dockerfile.train) é uma imagem NGC PyTorch que já fornece
# CUDA, cuDNN, NCCL, PyTorch, Apex, Transformer-Engine e FlashAttention.
# Aqui instalamos apenas o que o Megatron-LM (fork llm-jp/Megatron-LM) e a
# conversão para HuggingFace exigem além da base. NÃO fixamos torch aqui para
# não conflitar com a versão compilada da imagem base.
# [EN] The base (docker/Dockerfile.train) is an NGC PyTorch image that already provides
# CUDA, cuDNN, NCCL, PyTorch, Apex, Transformer-Engine and FlashAttention.
# Here we install only what Megatron-LM (fork llm-jp/Megatron-LM) and the
# HuggingFace conversion require beyond the base. We do NOT pin torch here so as
# not to conflict with the version compiled into the base image.

# [PT] Tokenizador SentencePiece usado pelo Megatron (--tokenizer-type SentencePieceTokenizer)
# [EN] SentencePiece tokenizer used by Megatron (--tokenizer-type SentencePieceTokenizer)
sentencepiece==0.2.0

# [PT] Conversão de checkpoints Megatron -> HuggingFace safetensors (Fase 2b §7.10)
# [EN] Conversion of Megatron checkpoints -> HuggingFace safetensors (Phase 2b §7.10)
transformers==4.46.3
accelerate==1.1.1
safetensors==0.4.5

# [PT] Pré-processamento de dados do Megatron (tools/preprocess_data.py)
# [EN] Megatron data preprocessing (tools/preprocess_data.py)
nltk==3.9.1
einops==0.8.0
regex==2024.11.6

# [PT] Monitoramento de treino | [EN] Training monitoring
wandb==0.18.7
tensorboard==2.18.0

# [PT] Leitura do corpus Parquet para gerar .bin/.idx do Megatron
# [EN] Reading the Parquet corpus to generate Megatron's .bin/.idx
pyarrow==17.0.0
