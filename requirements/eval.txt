# Manacá — Dependências da imagem GPU de AVALIAÇÃO (Fase 4: manacá-eval)
# ----------------------------------------------------------------------
# O framework manacá-eval (fork de llm-jp/llm-jp-eval) é um artefato da Fase 4
# e será instalado separadamente (pip install -e) quando existir. Esta imagem
# fornece o runtime de inferência (vLLM) e as bibliotecas de métricas PT-BR.

# Inferência acelerada para rodar os modelos avaliados
vllm==0.6.4.post1

transformers==4.46.3
datasets==3.1.0
sentencepiece==0.2.0
accelerate==1.1.1

# Métricas de avaliação (char_f1, rouge, pearson/STS, exact_match, etc.)
evaluate==0.4.3
sacrebleu==2.4.3
rouge-score==0.1.2
scikit-learn==1.5.2
scipy==1.14.1
pandas==2.2.3

# LLM-as-Judge e utilidades
openai==1.57.0
tqdm==4.67.1

# Benchmarks de multipla escolha por log-verossimilhanca (ARC-PT, HellaSwag-PT,
# LAMBADA-PT), no mesmo protocolo do Tucano (acc_norm / few-shot). Roda qualquer
# modelo HF. Ver scripts/eval/run_lm_eval_pt.sh.
lm-eval==0.4.5
